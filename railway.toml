[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node mvp/mock-server/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
numReplicas = 2
healthcheckPath = "/api/health"
healthcheckTimeout = 30
